description: Airoha External Serial Controller

compatible: "airoha,air-esc"

include: flash-controller.yaml

properties:
    partial-erase:
      type: boolean
      description: |
        If set, indicates that the NVMC supports partial erase of flash pages.
