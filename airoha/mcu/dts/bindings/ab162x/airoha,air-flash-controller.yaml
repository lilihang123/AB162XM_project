description: Airoha Flash Controller

compatible: "airoha,air-flash-controller"

include: flash-controller.yaml

properties:
    partial-erase:
      type: boolean
      description: |
        If set, indicates that the NVMC supports partial erase of flash pages.
